{% extends 'base.html.twig' %}

{% block content %}
    <div class="article">
        <p>{{ article.title }}</p>
        <p>{{ article.content }}</p>
        <p>{{ article.author }}</p>
        <p>{{ article.date.date|date('F  j  Y, H\\h i\\m') }}</p>
        {{ form_start(form) }}
            {{ form_errors(form) }}
            {{ form_label(form.comment, 'Comment', {'label_attr': {'class': 'text-primary'} }) }}
            {{ form_widget(form.comment) }}
            {{ form_errors(form.comment) }}
            {{ form_widget(form.save, { 'attr': {'class': 'btn btn-warnining' }) } }}
            {{ form_rest(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}