{% extends '::base_back.html.twig' %}

{% block content %}
    <h1 class="text-danger">Administrator : </h1>
    <a class="btn btn-info link-new-article" href="{{ path('back_role_new') }}">New role</a>
    <div class="table-responsive">
        <table class="table">
            <thead>
            <tr>
                <th>Id</th>
                <th>Role</th>
                <th>Actions</th>
            </tr>
            </thead>
            <tbody>
            {% for role in roles %}
                <tr>
                    <td>{{ role.id }}</td>
                    <td>{{ role.name|hook }}</td>
                    <td>
                        <a id="user-id" href="{{  path('back_role_update', { 'roleId': role.id })  }}" class="pull-left btn btn-warning">Update</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}